{
  "id": "07",
  "title": "Make a Toy",
  "blurb": "Four pieces of a custom node from editor to method.",
  "map": "07-make-a-toy.architecture.html",
  "chapters": [
    {
      "id": "intro",
      "title": "Hello",
      "focus": null,
      "text": "Every custom toy has four LEGO pieces: data struct, auth card, draw node, and run method. Grove glues them with numbers.",
      "deep": "Split across assemblies: unmanaged payload and enums in data or runtime, Grove Execution Auth or Data Auth in Authoring, Grove Execution Node in Editor, static Execute Node method in runtime."
    },
    {
      "id": "main-path",
      "title": "Four pieces",
      "focus": "main-path",
      "text": "Data holds Speed. Auth serializes it. Draw node is what you see in Graph Toolkit. Run method is the static C sharp with the attribute.",
      "deep": "Auth Node Type returns your enum int. Init writes blob fields via Blob Remap Builder. Editor Init copies inspector fields onto auth during import."
    },
    {
      "id": "helpers",
      "title": "Wire",
      "focus": "helpers",
      "text": "Importer copies draw fields into auth. Bake packs header plus payload. Use With Graph scopes the node to your map type.",
      "deep": "Create Node on auth allocates header and payload in the graph blob. Instance overrides patch serializable fields per entity before Create. Add Dependencies tracks extra assets."
    },
    {
      "id": "rooms",
      "title": "Play",
      "focus": "rooms",
      "text": "At play, Type hits your case in the door book. Method gets payload, Grove Context, and your context by ref. Then it mutates the world.",
      "deep": "Attribute Execute Node type id must match baked Type. Signature is public or internal static, non-generic outer type, ref context. Optional typed input for context and block nodes."
    }
  ],
  "nodes": {
    "auth_card": {
      "short": "Auth card. Grove*Auth ScriptableObject that Create/Init writes the brick piece. Not login.",
      "deep": "GroveExecutionAuth or GroveDataAuth. NodeType int, NodeId, Create allocates header+payload and BlobPtr kids via AuthUtil. Authoring layer only.",
      "label": "Auth card"
    },
    "type_enum": {
      "short": "Type number enum.",
      "deep": "Stable int shared by attribute and auth Node Type.",
      "label": "Type number"
    },
    "use_with_graph": {
      "short": "Use With Graph scope.",
      "deep": "Use With Graph typeof your Grove Graph.",
      "label": "UseWithGraph"
    },
    "draw_node": {
      "short": "Draw node in Toolkit.",
      "deep": "Grove Execution Node of Auth comma Data.",
      "label": "Draw node"
    },
    "brick_piece": {
      "short": "Baked header and payload.",
      "deep": "Header Type plus payload bytes in Graph Data.",
      "label": "Brick piece"
    },
    "importer": {
      "short": "Importer copies to auth.",
      "deep": "I Grove Node Create via Grove Node State.",
      "label": "Importer"
    },
    "door_book": {
      "short": "Generated switch case.",
      "deep": "Execution Generated case label.",
      "label": "Door book"
    },
    "overrides": {
      "short": "Per entity overrides.",
      "deep": "Graph Instance Instance Values list.",
      "label": "Overrides"
    },
    "data_struct": {
      "short": "Data struct. Unmanaged payload.",
      "deep": "Unmanaged struct living after Execution Header.",
      "label": "Data struct"
    },
    "run_method": {
      "short": "Static run method.",
      "deep": "Execute Node method body. Context by ref.",
      "label": "Run method"
    }
  }
}