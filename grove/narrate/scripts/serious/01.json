{
  "title": "The Toy Machine — internals",
  "map": "01-toy-machine.architecture.html",
  "chapters": [
    {
      "id": "intro",
      "title": "Contract",
      "view": null,
      "text": "Grove is a three-layer package: Editor authoring, Authoring bake, and runtime execution. You author a Graph Toolkit asset that derives Grove Graph. Import produces a Grove Auth Graph. Baking produces a Blob Asset Reference of Graph Data on an I Graph Reference component. Runtime never reads the source map."
    },
    {
      "id": "request-path",
      "title": "Pipeline",
      "view": "request-path",
      "text": "Pipeline: Grove Importer walks I Grove Nodes and subgraphs, sets Root when exactly one Input boundary resolves to an execution node, then Graph Authoring references the auth asset. Graph Baking Setup System reflects I Graph Baking types and creates Graph Baking System of T C. Auth Create builds the blob."
    },
    {
      "id": "identity-and-cache",
      "title": "Dispatch and state",
      "view": "identity-and-cache",
      "text": "Each baked node is a header of Type int plus Node Id, then an inline payload. Execution Header Execute calls context Execution Generated, a source-generated switch. Live graph memory is the Grove State buffer, a dynamic untyped hash map keyed by Graph Id so multi-graph entities do not clobber each other."
    },
    {
      "id": "async-work",
      "title": "Assemblies",
      "view": "async-work",
      "text": "Bovine Labs Grove Editor owns Toolkit nodes and import. Grove Authoring owns auth assets and bakers. Grove runtime owns Graph Impl, Graph Execution, built-in nodes, and the source generator contract. Your I System owns one Graph Impl and builds a Graph Execution each update after Set Chunk."
    }
  ],
  "nodes": {
    "users": "Source asset. Grove Graph subclass with a Graph attribute extension such as my graph.",
    "s3": "Editor assembly boundary. Graph Toolkit Graph Database assets.",
    "cdn": "Grove Importer Scripted Importer of T Auth. Creates auth subassets.",
    "lb": "Graph Baking System. Auto-spawned; hashes variants and instance overrides.",
    "api": "Blob Asset Reference Graph Data: Root, Inputs, Outputs, node maps, Graph Id.",
    "auth": "Node Type integer. Grove Execution Types negatives for built-ins; your positives for customs.",
    "cache": "Buffer container of Grove State. Get State extension returns the entity map.",
    "db": "I Context of T partial struct. Component and Buffer containers wired by the generator.",
    "queue": "Graph Impl Get Execution then job Set Chunk and Execute or Calculate.",
    "worker": "Static methods with Execute Node or Data Node attributes. Context by ref."
  }
}
