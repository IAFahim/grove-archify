{
  "title": "Push vs Pull \u2014 rebuild notes",
  "map": "06-push-pull.lifecycle.html",
  "chapters": [
    {
      "id": "intro",
      "title": "No full interpreter",
      "view": null,
      "text": "Runtime is recursive descent through blob pointers. Execute pushes control flow. Calculate pulls expressions. There is no system that visits every node each frame."
    },
    {
      "id": "main-lifecycle",
      "title": "Built-in control flow",
      "view": "main-lifecycle",
      "text": "CompositeData holds a BlobArray of ExecutionHeader pointers. Selectors keep parallel Qualifier and Action arrays; the last pair is the default threshold path; win conditions use strict greater-than on floats."
    },
    {
      "id": "human-waits",
      "title": "Data evaluation",
      "view": "human-waits",
      "text": "Outputs bind to DataHeaders. Variable and VariableSet are special-cased before generated dispatch. VariableSet re-evaluates and writes on every Calculate of its output."
    },
    {
      "id": "recovery-and-exits",
      "title": "GroveState keys",
      "view": "recovery-and-exits",
      "text": "GroveStateKey includes GraphId, optional scope, and key material from Hash128 splits for variables. GraphStateUtil.RemoveGraphState iterates keys and removes those with matching GraphId."
    }
  ],
  "nodes": {
    "queued": "GraphData.Root ExecutionHeader.",
    "planning": "GroveExecutionTypes.Composite runtime.",
    "executing": "Selector family under GroveExecutionTypes.",
    "reviewing": "Custom or built-in execution leaf.",
    "completed": "ECS mutations already applied this tick.",
    "approval": "GraphOutputData.Value data header.",
    "blocked": "DataHeader.Calculate path.",
    "failed": "Variable or graph-local state entry.",
    "cancelled": "Next system update boundary.",
    "expired": "Persistent GroveState buffer on entity."
  }
}
